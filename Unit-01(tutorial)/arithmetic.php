
<?php
    // Basic arithmetic operator

    $x =10;
    $y=2;
    $z = null;

    // $z = $x + $y;
    // $z = $x - $y;
    // $z = $x * $y;
    // $z = $x / $y;
    // $z = $x ** $y;      // power
    // $z = $x % $y;      // remainder

    echo "$z"; 

    // Increment & Decrement operator

    $counter = 5;

    // $counter ++ ;
    // $counter -- ;
    // $counter += 5;
    $counter -= 5;

    echo $counter;

    // operator precedence

    // ()
    // **
    // * / %
    // + - 
?>